{
  "name": "banner-animated",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && cp index.html index.css dist/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "gh-pages": "^6.0.0"
  }
}
